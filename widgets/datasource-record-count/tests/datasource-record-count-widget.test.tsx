import { React } from 'jimu-core'
import _Widget from '../src/runtime/widget'

describe('test datasource-record-count widget', () => {
  it('placeholder test', () => {
    expect(1).toEqual(1)
  })
})